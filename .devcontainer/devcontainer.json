{
  "name": "DATA 730 assignment",
  "image": "ghcr.io/prefix-dev/pixi:latest",
  "onCreateCommand": "apt-get update && apt-get install -y --no-install-recommends git openssh-client && rm -rf /var/lib/apt/lists/* && (pixi install --locked || pixi install)",
  "postAttachCommand": {
    "jupyterlab": "pixi run lab"
  },
  "forwardPorts": [8888],
  "portsAttributes": {
    "8888": {
      "label": "JupyterLab",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "reditorsupport.r"
      ]
    }
  }
}
