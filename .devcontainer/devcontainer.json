{
  "name": "DATA 730 assignment",
  "image": "ghcr.io/prefix-dev/pixi:latest",
  "containerEnv": {
    "TZ": "America/New_York"
  },
  "onCreateCommand": "apt-get update && apt-get install -y --no-install-recommends git openssh-client tzdata && rm -rf /var/lib/apt/lists/* && (pixi install --locked || pixi install)",
  "postAttachCommand": {
    "jupyterlab": "pixi run lab"
  },
  "forwardPorts": [8888],
  "portsAttributes": {
    "8888": {
      "label": "JupyterLab",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "reditorsupport.r"
      ]
    }
  }
}
